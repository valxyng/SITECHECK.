"use client"; import { useState } from "react"; import { useRouter } from "next/navigation";
export function UrlForm(){const [url,setUrl]=useState(""),[error,setError]=useState(""),router=useRouter();function submit(e:React.FormEvent){e.preventDefault();let v=url.trim();if(!/^https?:\/\//i.test(v))v=`https://${v}`;try{const parsed=new URL(v);if(!parsed.hostname)throw new Error();setError("");router.push(`/analyze?url=${encodeURIComponent(v.replace(/\/$/,""))}`)}catch{setError("Введите корректный URL")}}return <form onSubmit={submit} className="url-form"><div><span>↗</span><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com" aria-label="URL сайта"/><button>ПРОВЕРИТЬ САЙТ <b>→</b></button></div>{error?<p className="form-error">{error}</p>:<p className="form-note">Бесплатная базовая проверка · Без регистрации</p>}</form>}
